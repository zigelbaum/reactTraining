export default interface Product {
  id: string;
  code: number;
  name: string;
  description: string;
  price: number;
  category: string;
  amount: number;
  image: string;
  createdDate: string;
}